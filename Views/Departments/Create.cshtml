@model CDRMS_Web_Application.Models.DepartmentsModel

@{
    ViewData["Title"] = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1 class="mb-4 text-2xl font-bold">Create Department</h1>

<form asp-action="Create" class="rounded bg-white p-6 shadow-md">
    <div asp-validation-summary="ModelOnly" class="mb-4 text-red-500"></div>
    <div class="mb-5">
        <label asp-for="DepartmentName" class="mb-3 block text-lg font-medium text-gray-700">Department Name</label>
        <input asp-for="DepartmentName" class="w-full rounded border border-gray-300 px-4 py-2" placeholder="Enter Department Name" />
        <span asp-validation-for="DepartmentName" class="text-sm text-red-500"></span>
    </div>
    <div class="flex items-center space-x-4">
        <a asp-action="Index" class="rounded bg-gray-400 px-4 py-2 text-white hover:bg-gray-300">Back to List</a>
        <button type="submit" class="rounded bg-blue-500 px-4 py-2 text-white hover:bg-blue-600">Create</button>
    </div>
</form>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
