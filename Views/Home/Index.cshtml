@{
    ViewData["Title"] = "Home";

    var username = ViewData["Username"] as string;
    var email = ViewData["Email"] as string;
}

<div class="flex h-screen items-center justify-center bg-gray-100">
    <div class="rounded-lg bg-white p-8 shadow-lg transition-all hover:shadow-xl">
        <h1 class="text-2xl font-bold text-purple-600">
            Welcome, <span class="text-gray-800">@username</span>!
        </h1>
        <p class="mt-4 text-lg text-gray-600">Your personalized dashboard is here to make your day easier and more productive.</p>
        <p class="mt-2 text-lg text-gray-600">Let’s accomplish something amazing today!</p>
    </div>
</div>
